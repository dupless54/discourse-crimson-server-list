@AGENTS.md

# Claude Code adapter
Route only to the nearest local `AGENTS.md` for the current server-list surface. For probe/network work always read `lib/AGENTS.md` and `.agents/skills/project-security-review/SKILL.md`. Do not preload every game adapter or large UI component unless involved.
