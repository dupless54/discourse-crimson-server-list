# AI context system
`CURRENT_STATE.md` is the short checkpoint and `WORKFLOW.md` owns cross-model review roles. Multi-session work uses only `docs/ai/work/<feature>/{state.md,progress.md,implementation-plan.md}` unless a phase genuinely needs separate detail. Store facts, not chat transcripts.
