@AGENTS.md

# Gemini adapter
Treat `AGENTS.md` as canonical. Load only current state plus nearest local instructions/source. Network probing is security-sensitive; route to `lib/AGENTS.md` and the security skill when touched. Do not recursively preload the repo.
