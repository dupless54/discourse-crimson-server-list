# Crimson Server List schema
Read `.agents/skills/project-schema-review/SKILL.md` before migrations/index changes. Review uniqueness for votes/reviews/claims, ownership FKs, server-list browse/query indexes, probe-state growth, existing rows/backfills, lock/index cost, delete behavior, and recovery. Never run destructive production DB changes during development.
